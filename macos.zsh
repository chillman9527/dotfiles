# add path for network utils and metasploit framework
export PATH=$PATH:/usr/local/sbin:/opt/metasploit-framework/bin

# sets up proper alias commands when called
alias umount='diskutil unmount'
alias ptt='ssh bbsu@ptt.cc'
